apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: probe-rwx
  namespace: storage-selection-demo
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
  storageClassName: lvms-vg1
